# SPORT — הנחיות בסיס ומידע פרויקט

## מה זה הפרויקט
"הפורטל" של ניר הוא אתר סטטי אישי בשם **"המשחקים הבאים שלי"** — קובץ HTML יחיד (SPA), בעברית, RTL, ערכת נושא כהה. נכון ל-16/08/2026 הוא רץ רק מקומית (`file://`) על המחשב של ניר; בתהליך הפיכה ל-PWA שאפשר יהיה לאחסן באינטרנט ולהתקין בטלפון.

## מיקום הקבצים
במחשב של ניר (Windows): `C:\DFIR\Personal\Code\live-sports-main\`
- `index.html` — הפורטל עצמו.
- `manifest.json`, `sw.js`, `icon-192.png`, `icon-512.png`, `icon-512-maskable.png`, `apple-touch-icon.png` — קבצי PWA (נוספו 16/08/2026, ראו יומן שינויים).
- `CLAUDE.md` — קובץ הזיכרון הזה (עותק זהה לזה שבפרויקט הענן "SPORT").

כדי לגשת מסשן חדש: לבקש הרשאה לתיקייה הזו בדיוק דרך `device_request_folder_access` (ניר מעדיף נתיב ספציפי, לא תיקיות הורה רחבות — סירב פעם לבקשה כללית ל-Downloads).

## יומן שינויים (מה כבר נעשה)

**15/08/2026:**
1. הוסרו שני דפי "מונדיאל 2026" ו"הימורים" (`#wc-page`, `#bet-page`) לגמרי — כולל CSS, HTML, JS (`WC_MATCHES`, `renderWCMatches`, `wcFilter/wcSearch`, `buildBetTable`, `saveBets`, `loadSavedBets`, `BET_PEOPLE`).
2. נוסף דף **"משחקים באזור"** (`#region-page`, כפתור `btn-region`) — מדריך סטטי עם קישורים לאתרים הרשמיים, לכל קבוצות/ליגות הספורט הפעילות ב-DFW: 7 ליגות ראשיות, 13 משניות, 3 קולג', 2 מוטורי/גולף (`REGION_TEAMS` array, מסונן/מחופש לפי דרגה).
3. נוסף דף **"משחקים בחודש הקרוב"** (`#month-page`, כפתור `btn-month`) — תמונת-מצב סטטית (לא live) של משחקי בית אמיתיים בדאלאס בין 15/08–15/09/2026, עם תאריכים ויריבים בפועל במקום קישורים (`MONTH_GAMES` array). כולל: Texas Rangers (MLB), Dallas Trinity FC (USL Super League), Dallas Wings (WNBA), Dallas Cowboys (NFL, פרה-סיזון), FC Dallas (MLS), Frisco RoughRiders, North Texas SC, ו-3 קבוצות קולג' (SMU/TCU/North Texas) שפותחות את עונת הבית ב-12/9.
4. **נבדק במפורש ואומת** ש-NBA (Dallas Mavericks) ו-NHL (Dallas Stars) **אין להם שום משחק** בטווח 15/08–15/09/2026, ולכן הושמטו מדף "החודש הקרוב" בכוונה (לא פספוס):
   - NBA: כל הליגה פותחת preseason רק ב-3/10/2026, עונה סדירה ב-20/10/2026.
   - NHL/Stars: המשחק הראשון בבית (preseason) הוא רק ב-21/9/2026 (מול St. Louis Blues); לפני זה השחקנים בטורניר פרוספקטים בדטרויט (12-15/9).
5. תוקנה באג bidi: טווחי תאריכים עם מספרים (כמו "18–20 באוגוסט") בתוך HTML עם `dir="rtl"` היו מוצגים הפוך ("20–18"). התיקון: לעטוף את מחרוזת התאריך ב-`<span dir="ltr">...</span>`.
6. תוקן גם (תוך כדי מחקר) שם קבוצת ה-AUDL/UFA של דאלאס: שונה מ-Dallas Roughnecks ל-**Dallas Legion** (שם עדכני) בדף "משחקים באזור".

**16/08/2026 — הכנת PWA (התקנה כאפליקציה בטלפון):**
1. נוספו קבצי PWA לתיקיית הפורטל: `manifest.json` (שם, אייקונים, `display: standalone`, `theme_color` תואם לעיצוב הכהה), `sw.js` (service worker מינימלי — שומר רק את "מעטפת" האתר, לא נוגע בקריאות API חיצוניות כדי שנתוני הספורט תמיד יהיו חיים), וארבעה אייקונים (192/512/512-maskable/apple-touch, כולם בעיצוב כדורסל 🏀 שנוצר עם Playwright screenshot בהתאמה לצבעי הפורטל).
2. עודכן ה-`<head>` של `index.html`: קישור ל-manifest, meta theme-color, apple-touch-icon ותגי apple-mobile-web-app. נוסף script בסוף ה-body שרושם את ה-service worker.
3. **נבדק מקומית** עם שרת HTTP זמני (לא file://, כי service workers לא נרשמים דרך file://) — manifest נטען, כל האייקונים מחזירים 200, וה-service worker מגיע ל-`active`. אין שגיאות קונסול.
4. **הוגשה לניר** מסמך הוראות ("הוראות-אחסון-והתקנה.md") עם שתי דרכי אחסון חינמי (GitHub Pages כברירת מחדל / Netlify Drop לאפס-הרשמה) והוראות התקנה למסך הבית ב-Android/iOS.
5. הועלה לתשומת לב: יש קבוע `RAPIDAPI_KEY` לא בשימוש בקוד — ברגע שהאתר יהיה ציבורי כל אחד יוכל לראות אותו ב-View Source (זה קוד צד-לקוח). לא בעיה כרגע כי הוא לא בשימוש, אבל אם ירצה מפתח API אמיתי בעתיד, צריך פתרון אחר (לא לשים מפתחות סודיים בקוד לקוח).

**05/09/2026 — עדכון "החודש הקרוב" לטווח חדש + הוספת Real Madrid:**
1. עודכן דף **"משחקים בחודש הקרוב"** (`MONTH_GAMES`) לטווח חדש **15/09–15/10/2026** (הטווח הקודם, 15/08–15/09, פג תוקף). נבדקו לוחות זמנים רשמיים עדכניים דרך חיפוש (Dallas Cowboys, Texas Rangers, FC Dallas, Dallas Stars, Dallas Mavericks, TCU/SMU/North Texas Mean Green, ובדיקת מצב WNBA/USL/MiLB/MLS Next Pro).
2. שינויים מהותיים מול החודש הקודם:
   - **Dallas Stars (NHL) נכנסו לראשונה החודש**: 2 משחקי פרה-סיזון בבית (21/9 מול St. Louis Blues, 23/9 מול Minnesota Wild) **ופתיחת העונה הסדירה בבית ב-2/10** מול St. Louis Blues.
   - **Dallas Mavericks (NBA) נכנסו עם משחק פרה-סיזון יחיד בבית** ב-14/10 מול New Orleans Pelicans; פתיחת העונה הסדירה (21/10) היא משחק חוץ ומחוץ לטווח.
   - **Texas Rangers**: 2 סבבי בית אחרונים של העונה (15–19/9, מול Boston Red Sox ו-Toronto Blue Jays) ואז אין עוד משחקי בית — העונה הסדירה מסתיימת 27/9 בחוץ; מצב הפלייאוף לא ודאי נכון לתחילת ספטמבר (הקבוצה מאחורי בתחרות ה-Wild Card), ולוח משחקי בית לפלייאוף (אם יעפילו) טרם פורסם.
   - **Dallas Cowboys**: 2 משחקים (20/9 מול Washington Commanders, 8/10 מול Tampa Bay Buccaneers — חמישי, Amazon Prime).
   - **FC Dallas**: 2 משחקים (26/9 מול LAFC, 14/10 מול Seattle Sounders FC).
   - **הושמטו החודש** (נבדק ואומת שאין משחקי בית בטווח): Dallas Wings (העונה הסדירה מסתיימת 24/9, לא נמצאו משחקי בית בחלון הקצר שנותר), Dallas Trinity FC (הקוטון בול תפוס ע"י יריד המדינה של טקסס בספטמבר-אוקטובר — 4 משחקי חוץ רצופים, המשחק הבא בבית רק בנובמבר), Frisco RoughRiders (עונת המיליגה מסתיימת מוקדם בספטמבר, לא אומת תאריך מדויק — MiLB.com לא החזיר נתונים), North Texas SC (עברו למגרש חדש ב-Mansfield אך **לא נמצא לוח משחקים מפורט/מאומת לטווח הזה** — מומלץ לבדוק ידנית מול fcdallas.com/northtexassc בעדכון הבא).
3. **נוספה קטגוריה/תגית חדשה בדף "החודש הקרוב": "פורמולה 1 (משודר)"** — לפי בקשה מפורשת של ניר. 3 מרוצים שחלים בטווח: 26/9 גראנד פרי אזרבייג'ן (באקו), 4/10 גראנד פרי בחריין — בפועל מתקיים ב-Sepang, מלזיה (F1/FIA העבירו את המרוץ למלזיה ל-2026 אך שמרו על שם/חסות בחריין — שם מוזר בכוונה, אומת מול formula1.com), 11/10 גראנד פרי סינגפור. אלה **לא** משחקי בית בדאלאס — שידורי טלוויזיה בלבד; יש כפתור סינון נפרד וצבע כתום (`#f97316`) עקבי עם תגית ה-motorsport שכבר קיימת בדף "משחקים באזור".
4. **נוספה קבוצת Real Madrid לרשימת "אלה שאני עוקב אחריהם"** (`entries`, ליד FC Barcelona הקיימת), לפי בקשת ניר. אותו pattern (`fetchFootballNext`), ESPN soccer team id **86** (אומת מול espn.com/soccer/team/_/id/86).
5. תהליך העבודה: נעשה חיפוש אינטרנט (לא ידע מ-training) לכל הליגות/קבוצות, כולל שני סבבי אימות נפרדים (אחד ללוחות הזמנים הכלליים, אחד ממוקד לזהות מרוץ ה-F1 המסתורי ב-4/10). נבדק syntax (JS), נבדק render מקומי ב-Playwright headless (כולל אימות ה-bidi span על כל טווחי התאריכים החדשים), ואז `device_commit_files` לאותו נתיב.

**05/09/2026 — הכנה ל-GitHub + אבטחה:**
1. הוכן ריפו git מקומי (בסביבת הענן, לא על המחשב של ניר) עם כל קבצי הפורטל, `README.md` חדש, ו-`.gitignore`. ניר בחר לבצע בעצמו את יצירת הריפו ב-GitHub, ה-push, וההפעלה של GitHub Pages (לא לשתף Personal Access Token).
2. **אותרה ותוקנה בעיית אבטחה**: קבוע `RAPIDAPI_KEY` בקוד הכיל מפתח API אמיתי (לא placeholder) — `const RAPIDAPI_KEY = 'e4240bbf86msh17c6fd4e8bc8500p1668adjsn64c0418f9ba8';` — שהיה לא בשימוש בקוד אך עמד להיחשף לכל אחד ב-View Source ברגע שהאתר יהפוך לציבורי. הוסר (הוחלף במחרוזת ריקה עם הערה) גם בעותק המקומי של ניר וגם בעותק שהוכן לריפו, **לפני** ה-commit הראשון — כך שהמפתח לא נכנס להיסטוריית ה-git בכלל. **מומלץ לניר לשקול לבטל/להחליף את המפתח הזה בחשבון RapidAPI שלו** ליתר ביטחון, ליתר ביטחון (לא ידוע אם נחשף במקום אחר).
3. הוגשו לניר הוראות שלב-אחר-שלב ליצירת ריפו ב-GitHub, push של הקבצים המקומיים, והפעלת GitHub Pages (Settings → Pages → Deploy from branch → main → /root).

## פתוח / ממתין לפעולה
- **ניר בתהליך פרסום הפורטל ל-GitHub Pages בעצמו** (05/09/2026) — אחרי שיסיים ויש לו כתובת ציבורית (`https://<user>.github.io/<repo>/`), אפשר לעזור עם: (א) התקנה בטלפון, (ב) push/עדכון עתידי של קבצים (אם ניר ירצה שאעשה זאת ישירות דרך git, יצטרך לתת גישה — למשל Personal Access Token זמני, שיבוטל בסיום).
- ניר נשאל אם לרצה שדף "משחקים בחודש הקרוב" יציג גם NBA/NHL עם הערת "אין משחקים החודש, העונה נפתחת ב-X" במקום להשמיט אותן לגמרי — טרם ענה/טרם בוצע. אם מבקשים את זה בעתיד: להוסיף שתי כרטיסיות ל-`MONTH_GAMES` עם `games: []` ריק ושדה `note`, ולעדכן את `renderMonthGames()` שיטפל ברשימת games ריקה (כרגע מניחה שיש לפחות משחק אחד).
- דף "משחקים בחודש הקרוב" **יתיישן ב-15/10/2026** — כשמבקשים עדכון לחודש הבא: (א) לקבוע טווח תאריכים חדש, (ב) לבדוק מחדש אילו ליגות/קבוצות פעילות באותו טווח, (ג) לחפש לוחות זמנים עדכניים, (ד) לזכור את בעיית ה-bidi, (ה) לנסות שוב לאמת לוח משחקים ל-North Texas SC (לא הצליח באוקטובר 2026).
- קטגוריית "פורמולה 1 (משודר)" בדף "החודש הקרוב" היא תוספת חד-פעמית לפי בקשה — לא ברור אם ניר רוצה שהיא תישאר קבועה בכל עדכון חודשי הבא או רק כשיש מרוץ רלוונטי/הוא מבקש זאת שוב. לשאול אותו או להחליט לפי הקשר בעדכון הבא.

## מבנה הקוד (חשוב לפני כל עריכה עתידית)
- **ניווט (`showPage`)**: טאבים עליונים שמראים/מסתירים דפים (`div` עם class `visible`/`hidden`). כל דף חדש: כפתור ב-`.top-nav`, div לדף, מקרה ב-`showPage()`. דפים "כבדים" נבנים lazy (בלחיצה ראשונה) עם flag כמו `window._xBuilt`.
- **דף ראשי (main-page)**: "היום" (NBA/UCL/NFL/F1/NHL/MLB/NASCAR ותתי-סדרות/IndyCar/MotoGP), "אתמול", כרטיסיות אישיות (`entries` — כולל כעת גם FC Barcelona ו-Real Madrid), וסקשן "דאלאס השבוע" (`DALLAS_TEAMS` + `loadDallasWeek`, חי מ-ESPN, טווח 7 ימים).
- **דף "משחקים באזור" (`#region-page`)**: ראו יומן שינויים למעלה. `REGION_TEAMS` array, `renderRegionTeams`/`regionFilter`/`regionSearch`.
- **דף "משחקים בחודש הקרוב" (`#month-page`)**: ראו יומן שינויים למעלה. `MONTH_GAMES` array (כולל כעת גם tier `motorsport` ל-F1), `renderMonthGames`/`monthFilter`/`monthSearch`, `MONTH_TIER_LABEL`.
- **PWA**: `manifest.json` + `sw.js` + 4 אייקונים + תגיות ב-`<head>` + script רישום ה-SW לפני `</body>`. ה-SW לא שומר תגובות מ-origins חיצוניים (רק same-origin GET), כדי לא לפגוע בנתונים חיים.
- **מקורות נתונים חיים**: בעיקר ESPN's site.api.espn.com (ללא מפתח), וגם 365scores לכדורגל ישראלי. אין להשתמש ב-`RAPIDAPI_KEY` הקבוע בקוד — לא בשימוש בפועל (וגם: ברגע שהאתר ציבורי, מפתחות בקוד לקוח גלויים לכולם).
- **ליגות בלי API חינמי אמין** (NASCAR/Xfinity/Truck/IndyCar/MotoGP): מערך סטטי מראש (`XFINITY_2026` וכו') דרך `makeRaceWidget`. יש לעדכן ידנית עונה-עונה.
- **מודאל משחק (`openModal`)**: קומפוננטה משותפת לכל הכרטיסיות בכל הדפים.
- **אזור/עיר**: המיקוד תמיד **דאלאס-פורט וורת', טקסס** (שעון Texas = America/Chicago).

## טיפים טכניים חשובים
- **בעיית bidi בתאריכים**: טווח תאריכים עם מספרים (כמו "18–20 באוגוסט") בתוך HTML עם `dir="rtl"` — חובה לעטוף ב-`<span dir="ltr">...</span>`, אחרת הסדר מתהפך ("20–18"). ראו `renderMonthGames()`.
- **בדיקת service worker**: לא ניתן לבדוק רישום SW דרך `file://` (Chrome חוסם) — צריך שרת HTTP מקומי זמני (`python3 -m http.server`) כדי לבדוק לפני שליחה חזרה למחשב.
- **מחקר לוחות זמנים**: קריאות ישירות ל-ESPN API מסביבת הענן עלולות להיחסם ע"י ה-proxy הארגוני (`ERR_TUNNEL_CONNECTION_FAILED`/`connect_rejected`) — יש להסתמך על חיפוש אינטרנט (WebSearch/agent מחקר) ולא על קריאות API ישירות מהסנדבוקס בענן. בדיקת ה-render המקומי (Playwright + שרת HTTP) עדיין עובדת תקין למרות זה כי היא רק בודקת מבנה/HTML/JS, לא נתונים חיים.

## העדפות עבודה של ניר
- הפורטל חי **רק** על המחשב האישי שלו — אין repo מרוחק/GitHub שידוע לנו (עדיין). כל שינוי דרך ה-device bridge, לא GitHub.
- מעדיף לתת גישה **לנתיב ספציפי בלבד**, לא לתיקיות הורה.
- כשאפשר להניח החלטה בסבירות גבוהה — עדיף להציע ולנמק בקצרה, לא לשאול. כשיש כמה כיווני-פעולה שונים מהותית (למשל: אחסון חינמי מול remote-access מול אפליקציה נייטיבית) — כן לשאול לבחירה, כי ההשלכות שונות מאוד.
- ניר בודק/מטיל ספק בהיקף המחקר (למשל שאל אם NBA/NHL נלקחו בחשבון) — כדאי **תמיד לציין במפורש אילו ליגות/קבוצות/אפשרויות נבדקו ולמה הושמטו/נבחרו**, לא רק להציג את התוצאה הסופית.
- שינויים בקובץ: לבדוק syntax (JS) + render מקומי (Playwright headless, כולל בדיקת bidi בעברית!) לפני כתיבה חזרה, ואז `device_commit_files` לאותו נתיב.
- לעדכן את קובץ הזיכרון (גם בפרויקט וגם בתיקיית הפורטל עצמה) בכל פעם שנוספת יכולת משמעותית חדשה או שמתקבלת החלטה/העדפה חדשה מניר — וגם כשהוא מבקש זאת במפורש ("תעדכן קובץ זיכרון").
