# SPORT — הנחיות בסיס ומידע פרויקט

## מה זה הפרויקט
"הפורטל" של ניר הוא אתר סטטי אישי בשם **"המשחקים הבאים שלי"** — קובץ HTML יחיד (SPA), בעברית, RTL, ערכת נושא כהה. נכון ל-16/08/2026 הוא רץ רק מקומית (`file://`) על המחשב של ניר; בתהליך הפיכה ל-PWA שאפשר יהיה לאחסן באינטרנט ולהתקין בטלפון.

## מיקום הקבצים
במחשב של ניר (Windows): `C:\DFIR\Personal\Code\live-sports-main\`
- `index.html` — הפורטל עצמו.
- `manifest.json`, `sw.js`, `icon-192.png`, `icon-512.png`, `icon-512-maskable.png`, `apple-touch-icon.png` — קבצי PWA (נוספו 16/08/2026, ראו יומן שינויים).
- `CLAUDE.md` — קובץ הזיכרון הזה (עותק זהה לזה שבפרויקט הענן "SPORT").

כדי לגשת מסשן חדש: לבקש הרשאה לתיקייה הזו בדיוק דרך `device_request_folder_access` (ניר מעדיף נתיב ספציפי, לא תיקיות הורה רחבות — סירב פעם לבקשה כללית ל-Downloads).

## מצב נוכחי (תמצית, נכון ל-06/09/2026)
- **הפורטל עדיין רץ מקומית בלבד על המחשב של ניר** (`file://` / שרת מקומי). הוכן ריפו git מלא בסביבת הענן (לא על מחשבו) עם README/.gitignore/היסטוריית commits, ונמסרו לניר הוראות שלב-אחר-שלב ל-`git init`/`push`/הפעלת GitHub Pages — **לא ידוע אם ניר כבר ביצע את זה בפועל** (הוא בחר לעשות זאת בעצמו, לא לשתף credentials).
- **דפים קיימים**: ראשי ("היום"/"אתמול"/כרטיסיות מעקב אישיות/"דאלאס השבוע"), "משחקים באזור", "משחקים בחודש הקרוב" (MONTH_GAMES, מתיישן ב-15/10/2026), PWA מלא (manifest+SW+אייקונים).
- **ליגת המכללות (NCAA) נוספה בשני מקומות**: (1) ב-"דאלאס השבוע" — SMU/TCU/North Texas, פוטבול+כדורסל, נתונים חיים מ-ESPN מסוננים לשלוש הקבוצות המקומיות בלבד; (2) בחלק העליון ("היום") — 2 ריבועים ארציים "🏈 פוטבול מכללות" ו-"🏀 כדורסל מכללות" (לא מסוננים, בדיוק כמו NFL/NBA — עשויים להראות משחקים לא-דאלאסיים).
- **אבטחה**: מפתח RapidAPI אמיתי (`RAPIDAPI_KEY`) שהיה חשוף בקוד הוסר לפני כל commit — לא היה בשימוש בקוד. **טרם אושר שניר ביטל/החליף את המפתח בפועל אצל RapidAPI** — פעולה מומלצת שעדיין פתוחה.
- **תקלה טכנית שנפתרה**: כתיבות לקובץ (`index.html`, וגם `CLAUDE.md` עצמו) נראו "נעלמות" אחרי `device_commit_files` מוצלח, כי כלי Git GUI היה פתוח על המחשב של ניר וצפה בתיקייה. נפתר אחרי שניר סגר אותו. **כלל קבוע מעכשיו**: לוודא שאין כלי Git/עורך פתוח על התיקייה לפני כתיבה, ולאמת כל כתיבה בפועל (השוואת גודל/תוכן אחרי `device_stage_files` חוזר, כולל המתנה של כמה שניות) — לא להסתמך על "written: true" בלבד.

## יומן שינויים (מה כבר נעשה)

**15/08/2026:**
1. הוסרו שני דפי "מונדיאל 2026" ו"הימורים" (`#wc-page`, `#bet-page`) לגמרי — כולל CSS, HTML, JS (`WC_MATCHES`, `renderWCMatches`, `wcFilter/wcSearch`, `buildBetTable`, `saveBets`, `loadSavedBets`, `BET_PEOPLE`).
2. נוסף דף **"משחקים באזור"** (`#region-page`, כפתור `btn-region`) — מדריך סטטי עם קישורים לאתרים הרשמיים, לכל קבוצות/ליגות הספורט הפעילות ב-DFW: 7 ליגות ראשיות, 13 משניות, 3 קולג', 2 מוטורי/גולף (`REGION_TEAMS` array, מסונן/מחופש לפי דרגה).
3. נוסף דף **"משחקים בחודש הקרוב"** (`#month-page`, כפתור `btn-month`) — תמונת-מצב סטטית (לא live) של משחקי בית אמיתיים בדאלאס בין 15/08–15/09/2026, עם תאריכים ויריבים בפועל במקום קישורים (`MONTH_GAMES` array). כולל: Texas Rangers (MLB), Dallas Trinity FC (USL Super League), Dallas Wings (WNBA), Dallas Cowboys (NFL, פרה-סיזון), FC Dallas (MLS), Frisco RoughRiders, North Texas SC, ו-3 קבוצות קולג' (SMU/TCU/North Texas) שפותחות את עונת הבית ב-12/9.
4. **נבדק במפורש ואומת** ש-NBA (Dallas Mavericks) ו-NHL (Dallas Stars) **אין להם שום משחק** בטווח 15/08–15/09/2026, ולכן הושמטו מדף "החודש הקרוב" בכוונה (לא פספוס):
   - NBA: כל הליגה פותחת preseason רק ב-3/10/2026, עונה סדירה ב-20/10/2026.
   - NHL/Stars: המשחק הראשון בבית (preseason) הוא רק ב-21/9/2026 (מול St. Louis Blues); לפני זה השחקנים בטורניר פרוספקטים בדטרויט (12-15/9).
5. תוקנה באג bidi: טווחי תאריכים עם מספרים (כמו "18–20 באוגוסט") בתוך HTML עם `dir="rtl"` היו מוצגים הפוך ("20–18"). התיקון: לעטוף את מחרוזת התאריך ב-`<span dir="ltr">...</span>`.
6. תוקן גם (תוך כדי מחקר) שם קבוצת ה-AUDL/UFA של דאלאס: שונה מ-Dallas Roughnecks ל-**Dallas Legion** (שם עדכני) בדף "משחקים באזור".

**16/08/2026 — הכנת PWA (התקנה כאפליקציה בטלפון):**
1. נוספו קבצי PWA לתיקיית הפורטל: `manifest.json` (שם, אייקונים, `display: standalone`, `theme_color` תואם לעיצוב הכהה), `sw.js` (service worker מינימלי — שומר רק את "מעטפת" האתר, לא נוגע בקריאות API חיצוניות כדי שנתוני הספורט תמיד יהיו חיים), וארבעה אייקונים (192/512/512-maskable/apple-touch, כולם בעיצוב כדורסל 🏀 שנוצר עם Playwright screenshot בהתאמה לצבעי הפורטל).
2. עודכן ה-`<head>` של `index.html`: קישור ל-manifest, meta theme-color, apple-touch-icon ותגי apple-mobile-web-app. נוסף script בסוף ה-body שרושם את ה-service worker.
3. **נבדק מקומית** עם שרת HTTP זמני (לא file://, כי service workers לא נרשמים דרך file://) — manifest נטען, כל האייקונים מחזירים 200, וה-service worker מגיע ל-`active`. אין שגיאות קונסול.
4. **הוגשה לניר** מסמך הוראות ("הוראות-אחסון-והתקנה.md") עם שתי דרכי אחסון חינמי (GitHub Pages כברירת מחדל / Netlify Drop לאפס-הרשמה) והוראות התקנה למסך הבית ב-Android/iOS.
5. הועלה לתשומת לב: יש קבוע `RAPIDAPI_KEY` לא בשימוש בקוד — ברגע שהאתר יהיה ציבורי כל אחד יוכל לראות אותו ב-View Source (זה קוד צד-לקוח). לא בעיה כרגע כי הוא לא בשימוש, אבל אם ירצה מפתח API אמיתי בעתיד, צריך פתרון אחר (לא לשים מפתחות סודיים בקוד לקוח).

**05/09/2026 — עדכון "החודש הקרוב" לטווח חדש + הוספת Real Madrid:**
1. עודכן דף **"משחקים בחודש הקרוב"** (`MONTH_GAMES`) לטווח חדש **15/09–15/10/2026** (הטווח הקודם, 15/08–15/09, פג תוקף). נבדקו לוחות זמנים רשמיים עדכניים דרך חיפוש (Dallas Cowboys, Texas Rangers, FC Dallas, Dallas Stars, Dallas Mavericks, TCU/SMU/North Texas Mean Green, ובדיקת מצב WNBA/USL/MiLB/MLS Next Pro).
2. שינויים מהותיים מול החודש הקודם:
   - **Dallas Stars (NHL) נכנסו לראשונה החודש**: 2 משחקי פרה-סיזון בבית (21/9 מול St. Louis Blues, 23/9 מול Minnesota Wild) **ופתיחת העונה הסדירה בבית ב-2/10** מול St. Louis Blues.
   - **Dallas Mavericks (NBA) נכנסו עם משחק פרה-סיזון יחיד בבית** ב-14/10 מול New Orleans Pelicans; פתיחת העונה הסדירה (21/10) היא משחק חוץ ומחוץ לטווח.
   - **Texas Rangers**: 2 סבבי בית אחרונים של העונה (15–19/9, מול Boston Red Sox ו-Toronto Blue Jays) ואז אין עוד משחקי בית — העונה הסדירה מסתיימת 27/9 בחוץ; מצב הפלייאוף לא ודאי נכון לתחילת ספטמבר (הקבוצה מאחורי בתחרות ה-Wild Card), ולוח משחקי בית לפלייאוף (אם יעפילו) טרם פורסם.
   - **Dallas Cowboys**: 2 משחקים (20/9 מול Washington Commanders, 8/10 מול Tampa Bay Buccaneers — חמישי, Amazon Prime).
   - **FC Dallas**: 2 משחקים (26/9 מול LAFC, 14/10 מול Seattle Sounders FC).
   - **הושמטו החודש** (נבדק ואומת שאין משחקי בית בטווח): Dallas Wings (העונה הסדירה מסתיימת 24/9, לא נמצאו משחקי בית בחלון הקצר שנותר), Dallas Trinity FC (הקוטון בול תפוס ע"י יריד המדינה של טקסס בספטמבר-אוקטובר — 4 משחקי חוץ רצופים, המשחק הבא בבית רק בנובמבר), Frisco RoughRiders (עונת המיליגה מסתיימת מוקדם בספטמבר, לא אומת תאריך מדויק — MiLB.com לא החזיר נתונים), North Texas SC (עברו למגרש חדש ב-Mansfield אך **לא נמצא לוח משחקים מפורט/מאומת לטווח הזה** — מומלץ לבדוק ידנית מול fcdallas.com/northtexassc בעדכון הבא).
3. **נוספה קטגוריה/תגית חדשה בדף "החודש הקרוב": "פורמולה 1 (משודר)"** — לפי בקשה מפורשת של ניר. 3 מרוצים שחלים בטווח: 26/9 גראנד פרי אזרבייג'ן (באקו), 4/10 גראנד פרי בחריין — בפועל מתקיים ב-Sepang, מלזיה (F1/FIA העבירו את המרוץ למלזיה ל-2026 אך שמרו על שם/חסות בחריין — שם מוזר בכוונה, אומת מול formula1.com), 11/10 גראנד פרי סינגפור. אלה **לא** משחקי בית בדאלאס — שידורי טלוויזיה בלבד; יש כפתור סינון נפרד וצבע כתום (`#f97316`) עקבי עם תגית ה-motorsport שכבר קיימת בדף "משחקים באזור".
4. **נוספה קבוצת Real Madrid לרשימת "אלה שאני עוקב אחריהם"** (`entries`, ליד FC Barcelona הקיימת), לפי בקשת ניר. אותו pattern (`fetchFootballNext`), ESPN soccer team id **86** (אומת מול espn.com/soccer/team/_/id/86).
5. תהליך העבודה: נעשה חיפוש אינטרנט (לא ידע מ-training) לכל הליגות/קבוצות, כולל שני סבבי אימות נפרדים (אחד ללוחות הזמנים הכלליים, אחד ממוקד לזהות מרוץ ה-F1 המסתורי ב-4/10). נבדק syntax (JS), נבדק render מקומי ב-Playwright headless (כולל אימות ה-bidi span על כל טווחי התאריכים החדשים), ואז `device_commit_files` לאותו נתיב.

**05/09/2026 — הכנה ל-GitHub + אבטחה:**
1. הוכן ריפו git מקומי (בסביבת הענן, לא על המחשב של ניר) עם כל קבצי הפורטל, `README.md` חדש, ו-`.gitignore`. ניר בחר לבצע בעצמו את יצירת הריפו ב-GitHub, ה-push, וההפעלה של GitHub Pages (לא לשתף Personal Access Token).
2. **אותרה ותוקנה בעיית אבטחה**: קבוע `RAPIDAPI_KEY` בקוד הכיל מפתח API אמיתי (לא placeholder) — `const RAPIDAPI_KEY = 'e4240bbf86msh17c6fd4e8bc8500p1668adjsn64c0418f9ba8';` — שהיה לא בשימוש בקוד אך עמד להיחשף לכל אחד ב-View Source ברגע שהאתר יהפוך לציבורי. הוסר (הוחלף במחרוזת ריקה עם הערה) גם בעותק המקומי של ניר וגם בעותק שהוכן לריפו, **לפני** ה-commit הראשון — כך שהמפתח לא נכנס להיסטוריית ה-git בכלל. **מומלץ לניר לשקול לבטל/להחליף את המפתח הזה בחשבון RapidAPI שלו** ליתר ביטחון, ליתר ביטחון (לא ידוע אם נחשף במקום אחר).
3. הוגשו לניר הוראות שלב-אחר-שלב ליצירת ריפו ב-GitHub, push של הקבצים המקומיים, והפעלת GitHub Pages (Settings → Pages → Deploy from branch → main → /root).

**06/09/2026 — הוספת ליגת המכללות (NCAA, פוטבול+כדורסל) לסקשן "דאלאס השבוע":**
1. ניר ביקש להוסיף את ליגת המכללות "בדפים המתאימים". קבוצות הקולג' המקומיות (SMU, TCU, North Texas Mean Green) כבר היו קיימות כערכים סטטיים בדפי "משחקים באזור" ו"משחקים בחודש הקרוב" — לכן ההשלמה ההגיונית הייתה: הוספת נתונים **חיים** לאותן 3 קבוצות בסקשן "דאלאס השבוע" (`DALLAS_TEAMS`), באותו pattern בדיוק כמו Mavericks/Rangers/Cowboys/FC Dallas הקיימים (fetch מ-ESPN, סינון לשבוע קרוב).
2. **לא** נוסף ticker ארצי חדש ל"היום"/"אתמול" (כמו NBA/NFL) — כי scoreboard ארצי של NCAA Football/Basketball יחזיר עשרות משחקים בכל יום ללא סינון (יש כ-130 קבוצות FBS), מה שלא תואם את עיצוב הדף. הוחלט (לפי הקשר הקיים באתר — פוקוס דאלאס-פורט וורת') להתמקד רק בשלוש הקבוצות המקומיות, לא בכל הליגה הארצית או ב-Top 25.
3. נוספו 6 ערכים חדשים ל-`DALLAS_TEAMS`: פוטבול וכדורסל לכל אחת מ-SMU/TCU/North Texas (סה"כ 3 קבוצות × 2 ספורט). מזהי קבוצות ESPN אומתו בחיפוש אינטרנט (לא ניחוש): SMU=**2567**, TCU=**2628**, North Texas=**249** (אותו מזהה משותף לפוטבול וכדורסל בשתי הקבוצות, כמקובל ב-ESPN). אולמות כדורסל אומתו בנפרד מהאיצטדיונים (Moody Coliseum ל-SMU, Schollmaier Arena ל-TCU, UNT Coliseum/Super Pit ל-North Texas) — שונים מהאיצטדיונים של הפוטבול.
4. אנדפוינטים: `sports/football/college-football/teams/{id}/schedule` ו-`sports/basketball/mens-college-basketball/teams/{id}/schedule` (אותו API של ESPN, ללא מפתח, כמו שאר האתר). מציג משחקי בית וחוץ כאחד בטווח השבוע (כמו NBA/NFL, לא רק בית כמו MLB/MLS).
5. **נבדק**: syntax (JS, `node --check`), render מקומי (Playwright headless מול שרת HTTP זמני) — אין שגיאות קונסול מעבר לחסימת ה-proxy הארגוני על קריאות ESPN (צפוי, ראו "טיפים טכניים"). כדורסל קולג' עונתי (נובמבר עד מרץ בערך) — בספטמבר הסקשן פשוט לא יראה משחקי כדורסל עד שהעונה תיפתח, זה תקין.
6. `device_commit_files` לאותו נתיב.

**06/09/2026 (המשך) — תקלת "העלמות" השינוי + הוספת ריבועי "המשחק הבא" בחלק העליון:**
1. **אותרה תקלה חשובה**: כשניגשתי מחדש לקובץ (יזום ע"י בקשה נוספת של ניר), התברר שהשינוי מהסעיף הקודם (הוספת קבוצות הקולג' ל-`DALLAS_TEAMS`) **לא היה בקובץ בפועל** — הקובץ במחשב של ניר תאם בדיוק לגרסה **שלפני** אותו שינוי, למרות ש-`device_commit_files` דיווח בזמנו על הצלחה. הסיבה לא אותרה בוודאות (השערה סבירה: הקובץ היה פתוח בעורך במחשב של ניר ונשמר מחדש עם התוכן הישן על גבי הכתיבה שלי). **פעולה לזיכרון עתידי**: אחרי כל `device_commit_files` על קובץ קוד, **לאמת את ההצלחה מיידית** ע"י `device_stage_files` חוזר + השוואת תוכן/גודל בפועל — לא להסתמך רק על תשובת "written" מהכלי. אם ניר שם לב לתקלה דומה בעתיד, לשאול אם הקובץ פתוח לו בעורך (VS Code) בזמן הכתיבה.
2. הקוד של קבוצות הקולג' (מהסעיף הקודם) **נכתב מחדש** על בסיס הקובץ האמיתי שהיה במחשב, ואומת שוב (syntax + render).
3. **נוסף פיצ'ר חדש שביקש ניר**: 3 ריבועים (`daily-section`) בחלק העליון של הדף הראשי ("היום"), אחד לכל קבוצת קולג' מקומית — SMU/TCU/North Texas — בדפוס "הבא" (כמו NFL/F1), לא scoreboard יומי מלא. כל ריבוע (`loadNextCollegeTeam`) בודק גם פוטבול וגם כדורסל (אותו ESPN team id) ומציג את מה שקרוב יותר מבין השניים; אם רק אחד מהם רלוונטי (כרגע: כדורסל לא בעונה) — מוצג הפוטבול בלבד. **החלטה שהתקבלה לבד (לא נשאלה)**: ריבוע אחד משולב לכל קבוצה (לא נפרד לפוטבול/כדורסל) כדי לא להציף את החלק העליון שכבר מכיל 11 ריבועים — ניתן לפצל בעתיד אם ניר ירצה ריבוע נפרד לכל ספורט.
4. נבדק שוב syntax + render מקומי, ונכתב חזרה למחשב **עם אימות תוכן מיידי אחרי הכתיבה** (לא רק "written": true).

**06/09/2026 (המשך 2) — נמצא שורש הבעיה של "העלמות" השינויים + שינוי עיצוב לריבועים:**
1. **נמצא שורש הבעיה**: הכתיבה ל-`index.html` המשיכה "לחזור אחורה" גם אחרי הסעיף הקודם. ניר אישר שהיה לו **כלי Git פתוח** (כנראה GUI - GitHub Desktop/VS Code Source Control וכו') שצפה בתיקייה ושחזר את הקובץ. **ניר סגר את ה-Git**, ומאותו רגע הכתיבה נשארה יציבה (אומת עם המתנה של 8 שניות ובדיקה חוזרת). **לזיכרון עתידי: אם קובץ חוזר לגרסה קודמת אחרי `device_commit_files` מוצלח, לשאול קודם כל אם יש כלי Git/עורך פתוח שצופה בתיקייה** — זו הייתה הסיבה בפועל.
2. ניר ביקש שינוי עיצוב: **לא** ריבוע משולב לכל קבוצה מקומית (כמו שהוצע קודם), אלא **בדיוק כמו NFL/NBA** — כלומר 2 ריבועים ארציים (לא מסוננים לקבוצות מקומיות): "🏈 פוטבול מכללות — הבא" (`loadDailyNCAAF`, מבוסס 1:1 על `loadDailyNFL` — scoreboard להיום, fallback לסבב הקרוב אם אין משחקים היום, `slice(0,6)`), ו-"🏀 כדורסל מכללות — היום" (`loadDailyNCAAB`, מבוסס 1:1 על `loadDailyNBA`). **הוסרו** לגמרי הריבועים הקודמים (`loadNextCollegeTeam` + 3 ריבועי SMU/TCU/UNT נפרדים).
3. **הערה חשובה לעתיד**: הריבועים החדשים אינם מסוננים לקבוצות מקומיות (SMU/TCU/UNT) — הם מציגים scoreboard ארצי (כמו NFL/NBA), ולכן עשויים להראות משחקים לא-קשורים לדאלאס (יש כ-130 קבוצות FBS, וה-slice מציג רק 6 הראשונים מתוך הרשימה שה-API מחזיר). זו הייתה בחירה מפורשת של ניר ("בדיוק כמו NFL/NBA"), לא באג.
4. נבדק syntax + render מקומי, נכתב למחשב, **ואומת תוכן זהה גם מיד וגם אחרי המתנה של 8 שניות** (כדי לשלול revert מאוחר).

**15/09/2026 — סבב תיקון באגים (ביוזמת ניר, "בוא נטפל בכמה באגים"):**
1. **תוקן**: בכרטיסי "דאלאס השבוע" (`loadDallasWeek`) ה-`onclick='openModal(decodeURIComponent("${encoded}")')` היה חסר סוגריים סוגרים לפונקציית `decodeURIComponent` — שגיאת syntax ב-inline JS גרמה לכך שלחיצה על כל כרטיס (Mavericks/Rangers/Cowboys/FC Dallas/SMU/TCU/UNT) לא פתחה את המודאל בכלל. תוקן ל-`...decodeURIComponent("${encoded}"))'` (התאמה לתבנית התקינה שבשימוש בכל שאר הכרטיסים בקובץ).
2. **תוקן**: בתוך `openModal()`, שורת התוצאה הסופית של משחק שהסתיים (`${d.awayScore} – ${d.homeScore}`) לא הייתה עטופה ב-`dir="ltr"` — אותה בעיית bidi מוכרת (מספרים עם מקף בתוך HTML `dir="rtl"` מוצגים הפוך). תוקן לעטוף ב-`<div dir="ltr">` ולהשתמש ב-en dash (`–`) כמו בשאר מקומות התוצאה בקובץ (`loadYesterdayNBA`/`loadYesterdayUCL`).
3. **תוקן**: fetch של FC Dallas ב-`DALLAS_TEAMS` פנה ל-scoreboard endpoint עם `dates=` ליום בודד (היום בלבד) אבל סינן תוצאות לטווח של 7 ימים קדימה — בפועל ה-API מחזיר רק משחקי היום, כך שהסינון ל-7 ימים היה מת (FC Dallas כמעט לעולם לא היה מופיע ב"דאלאס השבוע" אלא אם יש משחק בדיוק היום). תוקן לטווח תאריכים ב-query (`dates=${txDateString(0)}-${txDateString(7)}`), בהתאם לפורמט הנתמך ע"י ESPN scoreboard.
4. **תהליך אימות**: node --check על שני בלוקי ה-`<script>`; רינדור מקומי עם Playwright headless מול שרת HTTP זמני (כל קריאות ה-ESPN/365scores נחסמו ע"י ה-proxy הארגוני של סביבת הענן כצפוי — ראו "טיפים טכניים"); הזרקת נתוני מודאל מדומים + לחיצה בפועל על כרטיס מדומה כדי לוודא שה-onclick המתוקן פותח את המודאל ושהניקוד מוצג נכון (`112–99` בתוך `dir="ltr"`) בלי שגיאות JS. נכתב למחשב עם `device_commit_files` ואומת תוכן זהה מיד אחרי (אין תקלת Git GUI הפעם).
5. שלושת התיקונים נמצאו ע"י audit ממוקד (לא דיווח ספציפי של ניר) — התבקש לבדוק בעצמו לפני שניר יכוון לבאג ספציפי.

**15/09/2026 (המשך) — 4 דיווחי באג נוספים מניר + 2 פיצ'רים חדשים:**
1. **תוקן**: ריבוע "🏈 NFL — הבא" (`loadDailyNFL`) — כשאין משחק היום, הקוד נפל חזרה ל-scoreboard בלי `dates=` (שמחזיר את כל השבוע הנוכחי, כולל משחקים שכבר הסתיימו) והציג את 6 האירועים הראשונים בלי סינון. בפועל זה גרם לריבוע "הבא" להראות רשימת משחקים שכולם מסומנים "(סיום)" גם כשאין משחק היום בכלל (ניר צירף צילום מסך). תוקן לסנן ל-חי/עתידי בלבד ולמיין לפי תאריך. **אותו באג בדיוק תוקן גם ב-`loadDailyNCAAF`** (שהיה מבוסס 1:1 על NFL, ראו יומן 06/09).
2. **תוקן**: ריבוע "🏎️ F1 — הבא" (`loadDailyF1`) קרא ל-scoreboard בלי טווח תאריכים, וזה מחזיר רק את השבוע הנוכחי — אם אין מרוץ השבוע הקרוב הריבוע הציג "אין מירוץ F1 קרוב" למרות שהעונה בעיצומה (יש מרוץ ב-26/9). תוקן לבקש טווח של 120 יום קדימה.
3. **נוסף**: ריבוע "🏈 NFL — אתמול" בסקשן "תוצאות אתמול" — היה קיים רק ל-NBA ול-UCL, NFL פשוט לא נכתב מעולם (לא "נעלם", חסר מהתחלה). נוסף `loadYesterdayNFL` באותו pattern בדיוק.
4. **תוקן**: ארבעת ריבועי המרוצים הסטטיים (NASCAR O'Reilly/Xfinity, NASCAR Truck, IndyCar, MotoGP) הציגו "סיים עונה" כי המערכים (`XFINITY_2026`/`TRUCK_2026`/`INDYCAR_2026`/`MOTOGP_2026`) הכילו רק חלק מהעונה (עד יולי-אוגוסט 2026) ולא עודכנו מאז. אומת מול nascar.com/indycar.com/motogp.com/wikipedia (ראו מקורות בהודעת הצ'אט):
   - **NASCAR O'Reilly (Xfinity) ו-Truck**: העונה האמיתית ממשיכה עד גמר הפלייאוף ב-Homestead-Miami (Xfinity: 7/11, Truck: 6/11) — נוספו כל מרוצי הפלייאוף. **זה לא היה "סוף עונה" אמיתי, רק נתונים חסרים.**
   - **IndyCar**: העונה **באמת הסתיימה** ב-6/9/2026 (גמר ב-Laguna Seca, מונטריי) — נוספו המרוצים החסרים עד הגמר (לתיעוד היסטורי), ונוסף פרמטר `nextSeasonNote` ל-`makeRaceWidget` שמציג עכשיו "העונה הסתיימה ב-6/9 · 2027 מתחילה ב-7/3/2027 (סנט פיטרסבורג)" במקום הודעה גנרית.
   - **MotoGP**: העונה ממשיכה עד ולנסיה ב-29/11/2026 — נוספו כל שאר הסבבים. **הערה**: זמן מרוץ קטאר (8/11) הוא הערכה (19:00 שעון מקומי, מרוץ לילה) — לא אומת מול הלוח הרשמי, כדאי לוודא סמוך למועד.
5. **תוקן (בקשת ניר)**: בדף "משחקים בחודש הקרוב", הכותרת הראשית אמרה "משחקים **בדאלאס** בחודש הקרוב" בזמן שהדף כולל גם מרוצי F1 שלא בדאלאס (באקו/מלזיה/סינגפור) — למרות שיש פסקת הסבר ותיוג ברור לכל כרטיס F1, הכותרת עצמה הטעתה. שונתה ל-"משחקי דאלאס + שידורי F1 בחודש הקרוב". (התוכן/הפילטור/התיוג לא השתנו — זו הייתה בחירה מפורשת של ניר מ-05/09, ראו יומן שינויים.)
6. **נוסף (בקשת ניר)**: ריבוע חדש "⚽ MLS — הבא" (`loadDailyMLS`) בסקשן "היום" הראשי — scoreboard ארצי של MLS, עם fallback לסבב הקרוב אם אין משחק היום (אותו pattern כמו NFL/NCAAF, כולל התיקון מסעיף 1 מלכתחילה).
7. **נוסף (בקשת ניר, "הקבוצה של מסי")**: נבדק ואומת מול ESPN/mlssoccer.com/intermiamicf.com — ליאו מסי עדיין ב-**Inter Miami CF** נכון ל-15/09/2026 (לא עבר קבוצה). נוסף לרשימת "אלה שאני עוקב אחריהם" (`entries`), ESPN team id **20232** (אומת מול espn.com), באותו pattern כמו שאר קבוצות הכדורגל (`fetchFootballNext`).
8. **תהליך אימות**: מחקר אינטרנט לכל לוחות הזמנים (nascar.com PDF רשמי, motogp.com, indycar.com/Wikipedia, espn.com לזיהוי קבוצות) — לא ידע מ-training; `node --check`; רינדור מקומי Playwright (כל הריבועים נבדקו שמציגים את המרוץ/משחק הנכון או הודעת שגיאה תקינה, בלי שגיאות JS; קריאות ESPN/365scores נחסמות כרגיל ע"י ה-proxy הארגוני). נכתב למחשב עם `device_commit_files`.
9. **תקלת חיבור (לא Git GUI הפעם, קרתה כמה פעמים ברצף בסבב הזה)**: באמצע העדכון הזה החיבור למחשב של ניר התנתק לרגע יותר מפעם אחת (שרתי remote-devices MCP התנתקו וחזרו) — כתיבות `device_commit_files` "הצליחו" לפי התשובה אבל בפועל לא נכתבו, גם ל-`index.html` וגם ל-`CLAUDE.md` עצמו (אומת כל פעם ע"י `device_stage_files` חוזר שהראה גרסה ישנה יותר מהצפוי). ניר נשאל במפורש אם יש כלי Git/עורך פתוח — התשובה הייתה לא, כך שהסיבה הייתה ניתוק זמני של חיבור remote-devices, לא Git GUI. **נוסף לזיכרון: תקלת חיבור MCP זמנית יכולה להיראות בדיוק כמו "כתיבה נעלמת" ועלולה לחזור כמה פעמים ברצף (כולל על CLAUDE.md עצמו!) — לבדוק תמיד עם `device_stage_files` + השוואת גודל/תוכן אחרי כל כתיבה בודדת, ולכתוב שוב מיד אם לא תואם, בלי להניח אוטומטית שזו בעיית Git.**

**15/09/2026 (המשך 2) — ניר הטיל ספק בהודעת "עונת הבין-עונות" של NFL, ובצדק:**
1. ניר שאל אם אני בטוח שאין משחקי NFL עכשיו ושזו "עונת הבין-עונות" — לפי ידיעתו היו משחקים בשבוע האחרון והעונה כבר נפתחה. **נבדק ואומת**: העונה הרגילה של NFL 2026 נפתחה ב-9/9/2026, ונכון ל-15/9 אנחנו ב-Week 2 של העונה הסדירה — ממש לא עונת הבין-עונות (אומת מול Wikipedia/nfl.com).
2. **נמצא הבאג**: התיקון שנעשה קודם בסבב הזה ל-`loadDailyNFL` (סעיף 1 למעלה) סינן נכון משחקים שהסתיימו, אבל עדיין הסתמך על קריאת fallback ל-scoreboard **בלי טווח תאריכים** — וזה מחזיר רק את השבוע הקלנדרי הנוכחי (א'-ש'). אם כל משחקי השבוע הזה כבר הסתיימו (למשל ביום שלישי-רביעי, אחרי Monday Night Football ולפני המשחק הבא בחמישי/ראשון), הקריאה הזו חוזרת ריקה — וזה גרם להצגת ההודעה הקבועה "אין משחקי NFL כעת (עונת הבין-עונות)", **שהיא שגויה עבור המצב הזה** (זה לא עונת בין-עונות, פשוט אין משחק בימים הקרובים ביותר). **אותו באג בדיוק קיים גם ב-`loadDailyNCAAF`** (מבוסס 1:1 על NFL).
3. **תוקן**: קריאת ה-fallback עכשיו מבקשת טווח תאריכים אמיתי (`dates=${txDateString(0)}-${txDateString(10)}`, 10 ימים קדימה) במקום להסתמך על "השבוע הנוכחי" של ESPN — כך שהיא תמיד תמצא את המשחק הבא בפועל, גם אם הוא בשבוע הקלנדרי הבא. גם נוסחי ההודעה שונו מ"עונת הבין-עונות" ל"אין משחקים קרובים בימים הקרובים" (ניטרלי, לא מניח למה אין משחק).
4. **לקח לזיכרון**: הודעת "עונת הבין-עונות" הייתה שגויה מלכתחילה ולא רק תוצאה של הבאג החדש — היא כתובה כטקסט קבוע בלי קשר לתאריך בפועל, וזה עלול לחזור בעתיד בכל ליגה שיש לה ימים ללא משחק בתוך העונה הפעילה (NBA, NHL, MLB בעונה עם ימי מנוחה). **כדאי לבדוק את כל הודעות ה"אין משחקים/עונת הבין-עונות" הדומות בקובץ** ולוודא שהן לא מניחות "עונת בין-עונות" רק כי אין תוצאות בשאילתה הראשונה/שנייה.
5. אומת: `node --check`, רינדור מקומי Playwright (קריאות ESPN חסומות כרגיל בסביבת הענן, מציג "שגיאה בטעינת NFL"/"שגיאה בטעינת פוטבול מכללות" - תקין, לא ניתן לאמת מול נתונים חיים אמיתיים מכאן). נכתב למחשב, ואומת תוכן זהה אחרי ניסיון שני (הראשון "נעלם" שוב מאותה תקלת חיבור MCP זמנית שתועדה למעלה - קרתה שוב באמצע העדכון הזה).

**16/09/2026 — audit מקיף (ביוזמת ניר, "תרוץ שוב על הכל... קוד, איפיונים, security") + תיקון P0/P1:**
1. ניר ביקש audit מלא: כל הבקשות/ההיסטוריה, כל הקוד, כל הריבועים, ורמת ה-security. הורצו שני צוותי בדיקה מקבילים (audit באגים מקיף + audit אבטחה), הממצאים רוכזו לתוכנית עבודה מסודרת (P0/P1/P2/P3) שנשמרה כמסמך בפרויקט "Sport-Portal" (`claude/תוכנית-עבודה-15-09-2026.md`). ניר אישר להתחיל מ-P0+P1 מיד.
2. **תוקן (P0)**: מערך `NASCAR_2026` (NASCAR Cup Series, בשימוש ע"י `loadDailyNASCAR`) — פוספס בעדכון הקודם (15/09) שהוסיף מרוצי פלייאוף רק ל-Xfinity/Truck/IndyCar/MotoGP. נוסף לוח הפלייאוף המלא (Round of 16/12/8 + Championship 4, ספטמבר-נובמבר 2026), מאומת מול nascar.com PDF רשמי.
3. **תוקן (P0)**: `fetchFootballNext` (המשמשת 7 מתוך 11 קבוצות/שחקנים ברשימת "אלה שאני עוקב אחריהם" - כולל מסי) לא סיננה משחקים עתידיים/לא-הסתיימו לפני לקיחת "המשחק הבא", בשונה משאר פונקציות ה-fetch (MLB/NFL/NBA). נוסף סינון זהה.
4. **תוקן (P1, באג פונקציונלי אמיתי + אבטחה)**: כל ה-onclick בקובץ (13 מקומות) נבנה כ-`onclick='openModal(decodeURIComponent("${encoded}"))'` כש-`encoded = encodeURIComponent(JSON.stringify(modalData))`. הבעיה: `encodeURIComponent` לא מקודד גרש (`'`), אבל התכונה עטופה בגרשיים בודדים — כל שם קבוצה עם גרש (כמו "Saint Mary's Gaels" בקולג' פוטבול/כדורסל האמיתי) שבר את הכרטיס בפועל, ופתח פוטנציאל להזרקת תכונת HTML זדונית. **תוקן** ע"י פונקציית עזר חדשה `encodeModalData()` שמקודדת גם גרשים ל-`%27` אחרי ה-encodeURIComponent הרגיל (decodeURIComponent עדיין מפענח נכון) — כל 13 המקומות הוחלפו ל-`encodeModalData(modalData)` בבת אחת (sed).
5. **תוקן (P1, אבטחה)**: נוספה פונקציית `escapeHtml()` והופעלה על כל שדה טקסט שמגיע מ-API חיצוני (ESPN/365scores) לפני הכנסה ל-`innerHTML`: בתוך `openModal()` (teams/score/detail/series/venue/city/tv rows), בכרטיסי "דאלאס השבוע" (`loadDallasWeek` — title/detail/city/label), ובריבוע F1 (`raceName`/`circuit`). זה סוגר גם את הבאג הפונקציונלי (שם קבוצה עם `<`/`"` היה שובר עיצוב) וגם וקטור XSS תיאורטי אם אי פעם יגיע HTML זדוני מ-API חיצוני.
6. **אימות**: נבנה תרחיש הזרקה בפועל ב-Playwright (שם קבוצה עם גרש + payload `onmouseover`, ו-payload `<img onerror>` בשדות שונים) ואומת שאף אחד מהם לא מצליח להזריק תכונה/להריץ קוד אחרי התיקון (לפני התיקון זה כן היה מצליח). `node --check` תקין, אין שגיאות JS ב-render מקומי.
7. **לא תוקן בסבב הזה (נשאר ל-P2/P3 בתוכנית העבודה)**: ריבועי המרוצים מדלגים ישר מ"עוד X שעות" ל"הסתיים" בלי מצב "בשידור חי" באמצע מרוץ; קוד מת משאריות פיצ'ר טניס (`fetchTennisNext`/`tennisTVUS`); שאר הריבועים (abbreviation-based, כמו NBA/MLB/NHL) לא עברו escapeHtml כי הם מציגים קודי קבוצה קצרים בשליטת ESPN (סיכון נמוך מאוד) — לא P1.
8. נכתב למחשב ואומת תוכן זהה (שוב הייתה תקלת חיבור זמנית פעם אחת באמצע — תוקן בניסיון חוזר).

**16/09/2026 (המשך) — תיקון P2 (ביוזמת ניר, "תתקן את P2 בבקשה"):**
1. **תוקן**: ריבועי המרוצים (NASCAR Cup/Xfinity/Truck, IndyCar, MotoGP) קפצו ישר מ"עוד X שעות/ימים" ל-"✅ הסתיים" ברגע ששעת ההתחלה עברה, גם אם המרוץ עדיין בשידור בפועל (מרוץ יכול לקחת 1.5-4 שעות). נוסף מיפוי `RACE_DURATION_HOURS` (משך משוער לכל סוג מרוץ: NASCAR Cup/Xfinity 3.5-4 שעות, Truck/IndyCar 3 שעות, MotoGP 1.5 שעות), ומצב חדש "🔴 בשידור חי" מוצג כל עוד הזמן הנוכחי בתוך חלון המשך המשוער מרגע ההתחלה. הוחל גם ב-`makeRaceWidget` (הפונקציה המשותפת ל-Xfinity/Truck/IndyCar/MotoGP) וגם בקוד הנפרד של `loadDailyNASCAR` (Cup Series). אומת עם בדיקת Playwright שמדמה זמנים שונים יחסית ל-"עכשיו" (2 שעות לפני / 30 דק' אחרי / 3 שעות אחרי / 5 שעות אחרי לגבי NASCAR; שעה/שעתיים אחרי לגבי MotoGP) ומוודאת שה-badge הנכון (ספירה לאחור / חי / הסתיים) מוצג בכל מקרה.
2. **הוסר**: קוד מת משאריות פיצ'ר טניס שלא היה בשימוש בשום מקום — הפונקציה `fetchTennisNext` (חיפוש שחקן/ית טניס ב-API של ESPN), הפונקציה `tennisTVUS` (מיפוי ערוצי שידור לטורנירי גרנד-סלאם), וכללי ה-CSS `.card.tennis::before` ו-`.card.tennis .sport-badge`. אומת עם grep שאין עוד אזכור של שלושתם בקובץ. **לא הוסרו** מיפויי `tennis` הכלליים ב-`SPORT_COLORS`/`SPORT_EMOJI`/`SPORT_LABEL` ובתווית הכרטיסיה (`sport-badge`) — אלה טבלאות גנריות משותפות לכל הספורטים (לא קוד ייעודי לטניס), ואינן חלק ממה שתועד כ"מת" ב-P2.
3. אומת: `node --check` על כל בלוקי ה-JS, ובדיקת Playwright ממוקדת שמדמה תרחישי זמן שונים ומאשרת את מצב ה"בשידור חי" (ראו סעיף 1). נכתב למחשב ואומת תוכן זהה — שוב נדרש ניסיון שני בגלל תקלת חיבור MCP זמנית (אותה תופעה מתועדת שוב, ראו הערה למעלה).
4. **נשאר פתוח (P3 בלבד כעת)**: מפתח RapidAPI (אישור ביטול/החלפה), סטטוס פרסום GitHub Pages, פיצ'ר "ליגות ריקות עם הערה" בעמוד החודשי, לוח משחקים ל-North Texas SC.

**16/09/2026 (המשך 2) — audit אבטחה נוסף ומעמיק (ביוזמת ניר, "תעשה ריויו נוסף ומעמיק... תראה שאין לנו בעיה מיוחדת"):**
1. הורץ סבב audit אבטחה עצמאי ואדוורסרי נוסף (agent נפרד, בלי הקשר לעבודה הקודמת) שקרא את כל הקובץ מקצה לקצה וניסה לתקוף אותו בפועל (jsdom, כי Playwright/Chromium לא הצליח להוריד בסביבה הזו הפעם - נעשה שימוש חלופי שמריץ את קוד האפליקציה האמיתי ובודק את ה-DOM שנוצר, מספיק כדי להוכיח escaping).
2. **נמצא פער אמיתי שהתיקון הקודם (P1, 16/09 מוקדם יותר) פספס**: `escapeHtml()` הופעל רק על `openModal()`, `loadDallasWeek()`, וריבוע F1 — אבל **לא** על `buildCard()` (הכרטיסיות "אלה שאני עוקב אחריהם" בראש העמוד — ליברפול/מסי/ברצלונה/ריאל וכו') וגם **לא** על 13 מקומות השורה `${away} @ ${home}` בכל ריבועי "היום"/"אתמול" (NBA/UCL/NHL/MLB/MLS/NFL/NCAAF/NCAAB). שמות קבוצות/ערוצי שידור שמגיעים מ-ESPN/365scores הוכנסו שם ל-`innerHTML` בלי escaping — באג מאותה משפחה בדיוק שכבר תוקנה במקומות אחרים, פשוט לא הוחלה בעקביות בכל מקום. **הערכת סיכון בפועל**: נמוכה (שדות ESPN בפועל הם קצרים ואלפאנומריים, לא נשלטים ע"י תוקף אנונימי, והאתר בלי login/עוגיות/PII כך שגם תרחיש הזרקה תיאורטי לא חושף כלום רגיש) — אבל תוקן בכל זאת כי זה תיקון זול ועקבי עם השאר.
3. **תוקן**: נוסף `escapeHtml()` על `match.vs`/`match.detail`/`item.region`/`item.channel` בתוך `buildCard()`, ועל כל 13 המקומות של `${away} @ ${home}` (כולל 3 מקומות ב"תוצאות אתמול" ששילבו `<strong>` להדגשת המנצח — שם ה-escaping מתבצע על השם עצמו לפני העטיפה ב-`<strong>`, כדי לא לשבור את התג). אומת עם grep שכל 13+3 המקומות תוקנו.
4. **תוקן (משני, hardening)**: נוספה תכונת `rel="noopener noreferrer"` לשני קישורי "הוסף ל-Google Calendar" (ב-`buildCard` וב-`openModal`) שהיו חסרים אותה (reverse-tabnabbing תיאורטי — היעד קבוע ל-calendar.google.com אז סיכון נמוך מאוד, אבל זול לתקן ועקבי עם קישור דומה בדף "משחקים באזור" שכבר כלל את זה).
5. **אומת מחדש כתקין (לא נמצאה בעיה)**: אין `eval`/`new Function`/setTimeout עם string; אין `document.write`/`outerHTML`; אין `javascript:` URLs; אין postMessage listeners; **אין שום שימוש ב-localStorage/sessionStorage/cookies בכל האתר** (אין התחברות, אין טפסים, אין PII נאסף כלל); אין `location.href` מבוסס קלט לא-מהימן (אין open-redirect); אין iframes; **אין שום CDN/script חיצוני** (הכל inline בקובץ אחד — אפס סיכון supply-chain/SRI); אין mixed-content אמיתי; `RAPIDAPI_KEY` מאומת כמחרוזת ריקה שלא בשימוש בשום מקום (שורה 1070) — אין מפתח אמיתי לחשוף. `manifest.json` תקין (scope/start_url יחסיים, אייקונים מקומיים בלבד). `sw.js` אומת כמתועד — שומר רק same-origin GET, לא נוגע בתשובות מ-ESPN/365scores.
6. **נבדק ולא תוקן (informational, לא נדרש לפני פרסום)**: אין תגית Content-Security-Policy. מכיוון שהקוד משתמש בהרבה `onclick='...'` inline, CSP מחמיר ידרוש `'unsafe-inline'` לסקריפטים או ריפקטור משמעותי ל-`addEventListener` — לא תוקן בסבב הזה, מוצע כשיפור עתידי (P2/P3) אם ירצה ניר, לא חובה.
7. **מסקנת ה-audit** (של סוכן הבדיקה, ואני מסכים): בהינתן מודל האיום בפועל — אתר סטטי לגמרי, בלי backend, בלי login, בלי PII, מיועד ל-GitHub Pages — **אין שום דבר שאמור לעכב פרסום**. הפער היחיד שנמצא (escapeHtml חסר בחלק מהמקומות) תוקן, לא היה ניתן לניצול בפועל ע"י תוקף אנונימי מלכתחילה (כי אין דרך להשפיע על מה ש-ESPN/365scores מחזירים), וגם בתרחיש כשל מקסימלי (אם ESPN/365scores יחזירו אי-פעם HTML מוזר) אין כלום רגיש לגנוב. הפורטל במצב אבטחה תקין וסביר למודל האיום שלו.
8. נכתב למחשב ואומת תוכן זהה (נדרשו שני ניסיונות ל-`index.html` בגלל אותה תקלת חיבור MCP זמנית מתועדת שוב — תוקן בניסיון חוזר, כרגיל).

**19/09/2026 — תיקון קריטי: RACE_DURATION_HOURS + באג אמיתי ב-NFL/NCAAF/MLS (ביוזמת ניר, דיווח על שגיאה בריבוע NASCAR Cup ועל "אין משחקי NFL" שגוי):**
1. ניר שלח צילום מסך של שגיאה בריבוע "NASCAR Cup — הבא": `Cannot access 'RACE_DURATION_HOURS' before initialization`, וגם דיווח שריבוע ה-NFL מציג "אין משחקים קרובים" למרות שהעונה בעיצומה ויש משחקים בסופ"ש (16/09-19/09).
2. **תוקן (הבאג מהתמונה)**: ב-`RACE_DURATION_HOURS` (נוסף בסבב ה-P2 של 16/09) הוגדר כ-`const` ליד `makeRaceWidget` (שורה ~2521 אז), אבל `loadDailyNASCAR()` — שמשתמש בו — נקרא הרבה קודם בסדר האתחול (שורה 1569, לפני שהסקריפט מגיע לשורת ה-`const`). ב-JS זה גורם ל-TDZ (temporal dead zone): `ReferenceError: Cannot access before initialization`. **פתרון**: הועבר `RACE_DURATION_HOURS` להיות מוגדר מוקדם בקובץ, מיד אחרי `TX_TZ` (שורה ~1405), הרבה לפני כל קריאת פונקציה שמשתמשת בו.
3. **נמצא ותוקן באג נפרד וחמור יותר (הדיווח על NFL), שקדם לתקלה הזו**: בדיקה חיה מול ה-API האמיתי של ESPN (WebFetch, לא רק grep על הקוד) גילתה ש-3 נקודות קצה (`football/nfl/scoreboard`, `football/college-football/scoreboard`, `soccer/usa.1/scoreboard`) **מחזירות שגיאת 400 על פרמטר טווח תאריכים `dates=YYYYMMDD-YYYYMMDD`** — בניגוד ל-`racing/f1/scoreboard` שבו הפרמטר הזה כן עובד (אומת בנפרד, 9 מרוצים חזרו כמצופה). זה אומר שכל "תיקון הטווח" שנעשה בסבבים קודמים (NFL/NCAAF ב-15-16/09, MLS ומשחקי FC Dallas) **מעולם לא עבד בפועל** — הקריאה נכשלת בשקט (400 → `res.ok=false` → מערך ריק), ולכן בכל פעם שאין משחק בדיוק היום, הריבוע הראה הודעת "אין משחקים" שגויה גם כשיש משחקים אמיתיים בימים הקרובים (בדיוק המקרה שניר תפס: יום שבת 19/9 בלי משחק, אבל יום ראשון 20/9 עם 5 משחקי NFL אמיתיים ומאומתים).
4. **פתרון כללי**: נוספה פונקציה משותפת חדשה `fetchScoreboardUpcoming(sportSlug, maxDaysAhead)` שמחפשת **יום-אחר-יום** עם קריאות `dates=YYYYMMDD` בודדות (מאומתות שעובדות בפועל בכל הליגות שנבדקו), במקום טווח אחד שנכשל. הוחלה ב: `loadDailyNFL` (fallback), `loadDailyNCAAF` (fallback), `loadDailyMLS` (fallback). **גם תוקן `FC Dallas` בריבוע "דאלאס השבוע"** (`DALLAS_TEAMS`) — היה משתמש באותה קריאת טווח שבורה, מה שאומר שמשחקי FC Dallas **לא הוצגו שם מעולם** מאז שהתיקון הראשוני נכתב (החזיר תמיד מערך ריק בשקט) — הוחלף בלולאת יום-אחר-יום דומה שסורקת 8 ימים ומסננת למשחקי FC Dallas (team id 14879) בלבד.
5. **אומת מול ה-API האמיתי (לא רק בסביבת הענן החסומה)**: `curl`/WebFetch ישירות ל-ESPN אישרו: (א) `dates=20260919` בודד ל-NFL מחזיר events ריק (נכון, אין משחק בשבת), (ב) `dates=20260920` בודד מחזיר 5 משחקי NFL אמיתיים ליום ראשון, (ג) `dates=20260919-20260929` (טווח) מחזיר 400 לכל אחד מ-NFL/NCAAF/MLS, (ד) אותו טווח עובד תקין ל-F1/racing (9 מרוצים). זו לא הייתה בדיקה תיאורטית — זו הייתה קריאה חיה בפועל ל-API האמיתי.
6. אומת גם עם Playwright: מוק (mock route) שמדמה בדיוק את ההתנהגות שאומתה מול ESPN (טווח=400, יום בודד=200) הראה שהריבוע מוצא נכון את המשחק העתידי הנכון (CAR@ATL) במקום הודעת "אין משחקים", וריבוע ה-NASCAR Cup נטען בלי שגיאת TDZ. `node --check` תקין.
7. **לקח לזיכרון חשוב**: קריאות API עם פרמטר טווח תאריכים (`dates=X-Y`) אינן נתמכות אוטומטית בכל נקודות הקצה של ESPN — זה תלוי בליגה/ספורט ספציפי, ולא ניתן להניח שזה עובד רק כי זה עובד בליגה אחרת. **מעכשיו: כל קריאת API חדשה עם טווח תאריכים חייבת להיבדק מול ה-API האמיתי (WebFetch/curl) לפני שהיא נחשבת מתוקנת** — לא מספיק node --check + Playwright עם קריאות חסומות, כי זה בדיוק מה שהחמיץ את הבאג הזה בשני סבבים קודמים (15/09, 16/09).
8. נכתב למחשב ואומת תוכן זהה (נדרש ניסיון שני ל-`index.html`, שוב אותה תקלת חיבור MCP זמנית).

**23/09/2026 — הוספת יומן שגיאות למשתמש (ביוזמת ניר: "כפתור בדף שבלחיצה עליו יפתח חלון עם הודעות שגיאה"):**
1. ניר ביקש דרך לראות בעצמו שיש בעיה בקריאות ה-API בלי לצטרך לשלוח צילומי מסך — במקום ריבוע קבוע בתחתית העמוד (שהיה תופס מקום קבוע גם כשאין שגיאות), הוחלט על כפתור 🐞 קטן קבוע בפינה (fixed position, פינה שמאלית תחתונה) שנפתח לחלון (modal, באותו סגנון עיצובי כמו חלון הפרטים הרגיל) רק בלחיצה.
2. **נבנה**: מערך גלובלי `APP_ERROR_LOG` (בזיכרון בלבד, מוגבל ל-50 רשומות אחרונות, מתאפס ברענון — לא ב-localStorage, כמתועד שהאתר לא משתמש באחסון דפדפן), פונקציה `logAppError(context, err)` שמוסיפה רשומה עם timestamp/הקשר/הודעה ומעדכנת badge אדום על הכפתור עם מספר השגיאות.
3. **חיווט**: `logAppError` נקרא מתוך **כל 14** בלוקי ה-`catch` הקיימים בקובץ (NBA/UCL/NHL/MLB/MLS יומי + NBA/UCL/NFL אתמול + NFL/NCAAF/NCAAB יומי + NASCAR Cup + F1 + הכרטיסיות האישיות ב-`buildCard` — שם רק על שגיאות אמיתיות, לא על "no_upcoming" שהוא מצב תקין), וגם על משחקים שנכשלו ב-`Promise.allSettled` בריבוע "דאלאס השבוע" (`loadDallasWeek`).
4. **נוסף גם תפיסת שגיאות גלובלית** (`window.addEventListener('error', ...)` + `('unhandledrejection', ...)`) שתופסת גם שגיאות שלא נתפסות בשום try/catch — בדיוק כמו הבאג של `RACE_DURATION_HOURS` מ-19/09 שלא היה קשור לשום קריאת API אלא לבאג בקוד עצמו. עם היומן הזה, באג כזה היה נראה מיד בכפתור, בלי לחכות שניר ישים לב וישלח צילום מסך.
5. חלון היומן: מציג כל שגיאה עם הקשר (איזו פונקציה/כרטיסיה), שעה, והודעת השגיאה (מוצגת דרך `escapeHtml()` — עקבי עם כל שאר הטקסט החיצוני בקובץ, גם אם בפועל הודעות שגיאה הן טקסט שאנחנו כותבים ולא טקסט חיצוני, נשמרה עקביות/הרגל בטוח). כפתור "נקה יומן" לאיפוס.
6. **אומת עם Playwright**: חסימת קריאות ל-ESPN (route abort) גרמה לכל ריבועי ה-fetch להיכשל, ואומת שכל 14 ההקשרים אכן מופיעים ביומן (למשל `loadDailyNBA` מופיע נכון); הזרקת שגיאה בלתי-תפוסה מלאכותית (`throw new Error`) ו-`Promise.reject` בלתי-מטופל אומתו כנתפסות ע"י ה-listeners הגלובליים ומופיעות ביומן; נבדק פתיחה/סגירה של החלון (X וגם קליק על הרקע), וכפתור הניקוי (מאפס את המספר ומחזיר "אין שגיאות רשומות"). `node --check` תקין.
7. נכתב למחשב ואומת תוכן זהה (נדרש ניסיון שני, שוב אותה תקלת חיבור MCP זמנית).

## פתוח / ממתין לפעולה
- **ניר בתהליך פרסום הפורטל ל-GitHub Pages בעצמו** (05/09/2026) — אחרי שיסיים ויש לו כתובת ציבורית (`https://<user>.github.io/<repo>/`), אפשר לעזור עם: (א) התקנה בטלפון, (ב) push/עדכון עתידי של קבצים (אם ניר ירצה שאעשה זאת ישירות דרך git, יצטרך לתת גישה — למשל Personal Access Token זמני, שיבוטל בסיום).
- ניר נשאל אם לרצה שדף "משחקים בחודש הקרוב" יציג גם NBA/NHL עם הערת "אין משחקים החודש, העונה נפתחת ב-X" במקום להשמיט אותן לגמרי — טרם ענה/טרם בוצע. אם מבקשים את זה בעתיד: להוסיף שתי כרטיסיות ל-`MONTH_GAMES` עם `games: []` ריק ושדה `note`, ולעדכן את `renderMonthGames()` שיטפל ברשימת games ריקה (כרגע מניחה שיש לפחות משחק אחד).
- דף "משחקים בחודש הקרוב" **יתיישן ב-15/10/2026** — כשמבקשים עדכון לחודש הבא: (א) לקבוע טווח תאריכים חדש, (ב) לבדוק מחדש אילו ליגות/קבוצות פעילות באותו טווח, (ג) לחפש לוחות זמנים עדכניים, (ד) לזכור את בעיית ה-bidi, (ה) לנסות שוב לאמת לוח משחקים ל-North Texas SC (לא הצליח באוקטובר 2026).
- קטגוריית "פורמולה 1 (משודר)" בדף "החודש הקרוב" היא תוספת חד-פעמית לפי בקשה — לא ברור אם ניר רוצה שהיא תישאר קבועה בכל עדכון חודשי הבא או רק כשיש מרוץ רלוונטי/הוא מבקש זאת שוב. לשאול אותו או להחליט לפי הקשר בעדכון הבא.

## מבנה הקוד (חשוב לפני כל עריכה עתידית)
- **ניווט (`showPage`)**: טאבים עליונים שמראים/מסתירים דפים (`div` עם class `visible`/`hidden`). כל דף חדש: כפתור ב-`.top-nav`, div לדף, מקרה ב-`showPage()`. דפים "כבדים" נבנים lazy (בלחיצה ראשונה) עם flag כמו `window._xBuilt`.
- **דף ראשי (main-page)**: "היום" (NBA/UCL/NFL/F1/NHL/MLB/NASCAR ותתי-סדרות/IndyCar/MotoGP), "אתמול", כרטיסיות אישיות (`entries` — כולל כעת גם FC Barcelona ו-Real Madrid), וסקשן "דאלאס השבוע" (`DALLAS_TEAMS` + `loadDallasWeek`, חי מ-ESPN, טווח 7 ימים).
- **דף "משחקים באזור" (`#region-page`)**: ראו יומן שינויים למעלה. `REGION_TEAMS` array, `renderRegionTeams`/`regionFilter`/`regionSearch`.
- **דף "משחקים בחודש הקרוב" (`#month-page`)**: ראו יומן שינויים למעלה. `MONTH_GAMES` array (כולל כעת גם tier `motorsport` ל-F1), `renderMonthGames`/`monthFilter`/`monthSearch`, `MONTH_TIER_LABEL`.
- **PWA**: `manifest.json` + `sw.js` + 4 אייקונים + תגיות ב-`<head>` + script רישום ה-SW לפני `</body>`. ה-SW לא שומר תגובות מ-origins חיצוניים (רק same-origin GET), כדי לא לפגוע בנתונים חיים.
- **מקורות נתונים חיים**: בעיקר ESPN's site.api.espn.com (ללא מפתח), וגם 365scores לכדורגל ישראלי. אין להשתמש ב-`RAPIDAPI_KEY` הקבוע בקוד — לא בשימוש בפועל (וגם: ברגע שהאתר ציבורי, מפתחות בקוד לקוח גלויים לכולם).
- **ליגות בלי API חינמי אמין** (NASCAR/Xfinity/Truck/IndyCar/MotoGP): מערך סטטי מראש (`XFINITY_2026` וכו') דרך `makeRaceWidget`. יש לעדכן ידנית עונה-עונה.
- **מודאל משחק (`openModal`)**: קומפוננטה משותפת לכל הכרטיסיות בכל הדפים.
- **אזור/עיר**: המיקוד תמיד **דאלאס-פורט וורת', טקסס** (שעון Texas = America/Chicago).

## טיפים טכניים חשובים
- **בעיית bidi בתאריכים**: טווח תאריכים עם מספרים (כמו "18–20 באוגוסט") בתוך HTML עם `dir="rtl"` — חובה לעטוף ב-`<span dir="ltr">...</span>`, אחרת הסדר מתהפך ("20–18"). ראו `renderMonthGames()`.
- **בדיקת service worker**: לא ניתן לבדוק רישום SW דרך `file://` (Chrome חוסם) — צריך שרת HTTP מקומי זמני (`python3 -m http.server`) כדי לבדוק לפני שליחה חזרה למחשב.
- **מחקר לוחות זמנים**: קריאות ישירות ל-ESPN API מסביבת הענן עלולות להיחסם ע"י ה-proxy הארגוני (`ERR_TUNNEL_CONNECTION_FAILED`/`connect_rejected`) — יש להסתמך על חיפוש אינטרנט (WebSearch/agent מחקר) ולא על קריאות API ישירות מהסנדבוקס בענן. בדיקת ה-render המקומי (Playwright + שרת HTTP) עדיין עובדת תקין למרות זה כי היא רק בודקת מבנה/HTML/JS, לא נתונים חיים.

## העדפות עבודה של ניר
- **חוק קשיח: לענות תמיד בעברית, אף פעם לא באנגלית** — כולל בטקסט התשובה עצמה וגם בתיאורי הפעולות הטכניות (tool descriptions) שרואים בממשק. זה הופר כמה פעמים בטעות בסשן אחד (05–06/09/2026), ניר דרש במפורש שזה ייפסק, וזה נרשם גם ב-`/preferences.md` הכללי. אין להתווכח או "לערער" מול ניר כשהוא מצביע על טעות מתועדת (למשל צילום מסך) — פשוט להודות ולתקן.
- הפורטל בתהליך מעבר מ"חי רק על המחשב האישי" ל-repo ב-GitHub + GitHub Pages (החל 05/09/2026, ראו יומן שינויים). ניר מעדיף לבצע בעצמו פעולות שדורשות אימות מול חשבון ה-GitHub שלו (יצירת ריפו, push, הפעלת Pages) — לא לשתף credentials/tokens בברירת מחדל.
- מעדיף לתת גישה **לנתיב ספציפי בלבד**, לא לתיקיות הורה.
- כשאפשר להניח החלטה בסבירות גבוהה — עדיף להציע ולנמק בקצרה, לא לשאול. כשיש כמה כיווני-פעולה שונים מהותית (למשל: אחסון חינמי מול remote-access מול אפליקציה נייטיבית) — כן לשאול לבחירה, כי ההשלכות שונות מאוד.
- ניר בודק/מטיל ספק בהיקף המחקר (למשל שאל אם NBA/NHL נלקחו בחשבון) — כדאי **תמיד לציין במפורש אילו ליגות/קבוצות/אפשרויות נבדקו ולמה הושמטו/נבחרו**, לא רק להציג את התוצאה הסופית.
- שינויים בקובץ: לבדוק syntax (JS) + render מקומי (Playwright headless, כולל בדיקת bidi בעברית!) לפני כתיבה חזרה, ואז `device_commit_files` לאותו נתיב.
- לעדכן את קובץ הזיכרון (גם בפרויקט וגם בתיקיית הפורטל עצמה) בכל פעם שנוספת יכולת משמעותית חדשה או שמתקבלת החלטה/העדפה חדשה מניר — וגם כשהוא מבקש זאת במפורש ("תעדכן קובץ זיכרון").
- **אם כתיבה לקובץ נראית "לא נתפסת"** (הקובץ חוזר לגרסה קודמת אחרי `device_commit_files` שדיווח הצלחה) — לשאול קודם כל אם יש כלי Git GUI או עורך פתוח שצופה בתיקייה (זו הייתה הסיבה בפועל ב-06/09/2026), ולאמת כל כתיבה עתידית בפועל (לא רק "written: true").
